; Config_Ellipse
;
PATH_CATALOGUE = '../prep_morpho/'
PATH_DATA = '../prep_morpho/'  ;;; images on which IRAF ellipse task is applied
CATALOGUE_INPUT = 'catalogue_morpho.cat'

SUFFIXE_im='_obj.fits'

;; IRAF files
PATH_ELLIPSE=''

;;; surface brigthness limit value used to compute the E and PA of the
;;; galaxy from the
;;; external ellipse isophote 
SB_limit=20.85  ;;; sky surface brightness 
